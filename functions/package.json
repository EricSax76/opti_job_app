{
  "name": "opti-job-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "build": "tsc -p tsconfig.json",
    "test:e2e": "npm run build && firebase emulators:exec --config ../firebase.json --project demo-opti-job --only firestore \"node --test e2e/*.test.js\"",
    "migrate:applications": "node scripts/migrate-applications-schema.js"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.4.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
