<section class="cta section">
  <div class="container">
    <div class="cta-card">
      <div class="cta-content">
        <h2 class="cta-title">
          ¿Listo para dar<br />
          <span class="text-gradient">el próximo paso?</span>
        </h2>
        <p class="cta-description">
          Únete a OptiJob y descubre cómo la inteligencia artificial puede
          ayudarte en tu búsqueda de empleo o talento
        </p>
        <div class="cta-buttons">
          <a href="/registro" class="btn btn-primary btn-lg">
            Comenzar gratis
          </a>
          <a href="/contacto" class="btn btn-secondary btn-lg"> Contactar </a>
        </div>
      </div>

      <div class="cta-highlight">
        <div class="highlight-item">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M5 13L9 17L19 7"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
          <span>Sin permanencia</span>
        </div>
        <div class="highlight-item">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M5 13L9 17L19 7"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
          <span>Cancela cuando quieras</span>
        </div>
        <div class="highlight-item">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M5 13L9 17L19 7"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
          <span>Soporte en español</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .cta {
    background: var(--color-bg-primary);
    position: relative;
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }

  .cta-card {
    position: relative;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-content {
    margin-bottom: var(--space-3xl);
  }

  .cta-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-lg);
    letter-spacing: -0.01em;
    color: var(--color-text-primary);
  }

  .cta-description {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-2xl);
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-highlight {
    display: flex;
    gap: var(--space-2xl);
    justify-content: center;
    flex-wrap: wrap;
    padding-top: var(--space-2xl);
    border-top: 1px solid var(--color-border);
  }

  .highlight-item {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    color: var(--color-text-secondary);
    font-size: var(--text-sm);
  }

  .highlight-item svg {
    color: var(--color-secondary);
    flex-shrink: 0;
  }
</style>
