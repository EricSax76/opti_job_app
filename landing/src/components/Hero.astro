<section class="hero section">
  <div class="container">
    <div class="hero-content">
      <div class="hero-label">
        <svg
          width="20"
          height="20"
          viewBox="0 0 20 20"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M10 3L12 7H17L13.5 10.5L15 17L10 13.5L5 17L6.5 10.5L3 7H8L10 3Z"
            fill="currentColor"></path>
        </svg>
        <span>Potenciado por IA</span>
      </div>

      <h1 class="hero-title">
        Encuentra el trabajo que mereces<br />
        con la ayuda de la <span class="text-gradient"
          >inteligencia artificial</span
        >
      </h1>

      <p class="hero-subtitle">
        OptiJob conecta candidatos con empresas de forma inteligente. Sin largos
        procesos. Sin candidaturas perdidas. Solo matches precisos.
      </p>

      <div class="hero-cta">
        <a href="/candidatos" class="btn btn-primary btn-lg">
          Comenzar como candidato
        </a>
        <a href="/empresas" class="btn btn-secondary btn-lg">
          Publicar oferta
        </a>
      </div>

      <div class="hero-stats">
        <div class="stat">
          <div class="stat-value">98%</div>
          <div class="stat-label">Precisión en matches</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <div class="stat-value">&lt;24h</div>
          <div class="stat-label">Respuesta media</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat">
          <div class="stat-value">100%</div>
          <div class="stat-label">Gratis para candidatos</div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    align-items: center;
    background: linear-gradient(to bottom, #ffffff, var(--color-bg-secondary));
  }

  .hero-content {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }

  .hero-label {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-xs) var(--space-md);
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-primary);
    margin-bottom: var(--space-xl);
  }

  .hero-label svg {
    width: 16px;
    height: 16px;
  }

  .hero-title {
    font-size: var(--text-4xl);
    font-weight: 700;
    margin-bottom: var(--space-xl);
    letter-spacing: -0.02em;
    line-height: 1.2;
    color: var(--color-text-primary);
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    margin-bottom: var(--space-3xl);
    line-height: 1.6;
  }

  .hero-cta {
    display: flex;
    gap: var(--space-md);
    margin-bottom: var(--space-4xl);
    justify-content: center;
    flex-wrap: wrap;
  }

  .hero-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2xl);
    padding-top: var(--space-2xl);
    border-top: 1px solid var(--color-border);
    flex-wrap: wrap;
  }

  .stat {
    text-align: center;
  }

  .stat-value {
    font-size: var(--text-2xl);
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-xs);
  }

  .stat-label {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .stat-divider {
    width: 1px;
    height: 32px;
    background: var(--color-border);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 80vh;
    }

    .hero-title {
      font-size: var(--text-3xl);
    }

    .stat-divider {
      display: none;
    }

    .hero-stats {
      flex-direction: column;
      gap: var(--space-lg);
    }
  }
</style>
