import 'package:flutter/material.dart';
import 'package:opti_job_app/features/video_curriculum/view/containers/video_curriculum_container.dart';

class VideoCurriculumScreen extends StatelessWidget {
  const VideoCurriculumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const VideoCurriculumContainer();
  }
}
